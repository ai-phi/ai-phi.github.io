---
import { SITE, LOCALE } from "@config";

interface Props {
  title: string;
  eyebrow?: string;
  date?: string | Date;
  author?: string;
  description?: string;
  ogImage?: string;
  href: string;
  expanded?: boolean;
}

const {
  title,
  eyebrow,
  date,
  author = SITE.author ?? "ai-phi",
  description,
  ogImage,
  href,
  expanded = false,
} = Astro.props as Props;

const fallbackImage = "/og/ai-phi-default-og.webp";
const resolvedImage =
  typeof ogImage === "string" && ogImage.length > 0 ? ogImage : fallbackImage;

const dateValue = date ? new Date(date) : null;
const displayDate = dateValue
  ? dateValue.toLocaleDateString(LOCALE.langTag, {
      year: "numeric",
      month: "short",
      day: "numeric",
      timeZone: LOCALE.timeZone ?? "Europe/Paris",
    })
  : "";
const displayTime = dateValue
  ? dateValue.toLocaleTimeString(LOCALE.langTag, {
      hour: "2-digit",
      minute: "2-digit",
      hour12: false,
      timeZone: LOCALE.timeZone ?? "Europe/Paris",
    })
  : "";

---

<article
  class:list={[
    "group relative h-full cursor-pointer rounded-2xl border border-skin-line/40 bg-[rgb(var(--color-card-muted))] p-5 shadow-lg shadow-black/10 transition-shadow transition-transform hover:-translate-y-0.5 hover:shadow-xl",
    expanded
      ? "flex flex-col gap-5 md:grid md:grid-cols-[minmax(0,1.15fr)_minmax(0,1fr)] md:items-stretch"
      : "flex flex-col",
  ]}
  data-card-link={href}
  tabindex="0"
  role="link"
>
  <div class:list={["w-full bg-[rgb(var(--color-card-muted))]", expanded && "h-full"]}>
    <img
      src={resolvedImage}
      alt="Illustration"
      class:list={[
        "h-full w-full rounded-lg object-cover",
        expanded ? "aspect-[16/9] md:aspect-auto md:min-h-full" : "",
      ]}
      loading="lazy"
      decoding="async"
    />
  </div>


  <div
    class:list={[
      "relative z-20 flex h-full select-text flex-col",
      expanded ? "md:justify-center" : "mt-4",
    ]}
  >
    {
      eyebrow && (
        <p class="text-xs font-semibold uppercase tracking-[0.2em] text-skin-base opacity-80">
          {eyebrow}
        </p>
      )
    }
    <h3
      class="text-2xl font-semibold leading-tight text-skin-base transition-colors duration-150 group-hover:text-3xl"
    >
      <a
        href={href}
        class="relative z-30 rounded-sm focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-skin-accent"
      >
        {title}
      </a>
    </h3>

    {
      dateValue && (
        <p class="mt-2 text-base font-medium text-skin-base opacity-80">
          <time datetime={dateValue.toISOString()}>{displayDate}</time>
          <span aria-hidden="true"> · </span>
          <span class="text-nowrap">{displayTime}</span>
          <span aria-hidden="true"> · </span>by {author}
        </p>
      )
    }

    {
      description && (
        <p
          class:list={[
            "mt-3 grow text-base leading-relaxed text-skin-base opacity-80 text-justify",
            expanded && "md:max-w-[52ch]",
          ]}
        >
          {description}
        </p>
      )
    }

    <!-- Visual CTA -->
    <div class="mt-auto pt-4">
      <a
        href={href}
        class="inline-flex items-center gap-1 font-medium transition-all duration-150
             group-hover:font-bold text-skin-base cursor-pointer relative z-30 rounded-sm focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-skin-accent"
      >
        Check details
        <svg
          xmlns="http://www.w3.org/2000/svg"
          class="h-4 w-4 transition-transform duration-150 group-hover:translate-x-0.5"
          viewBox="0 0 24 24"
          fill="currentColor"
          aria-hidden="true"
        >
          <path d="m13 5 7 7-7 7M4 12h16"></path>
        </svg>
      </a>
    </div>
  </div>
</article>

<script is:inline>
  const isInteractiveElement = (element, card) => {
    if (!(element instanceof HTMLElement)) return false;
    const interactiveAncestor = element.closest(
      'a, button, input, textarea, select, summary, [role="button"], [role="link"]'
    );
    return !!interactiveAncestor && interactiveAncestor !== card;
  };

  const hasSelectedText = () => {
    const selection = window.getSelection();
    return !!selection && selection.toString().trim().length > 0;
  };

  const activateCard = card => {
    const href = card.dataset.cardLink;
    if (href) window.location.assign(href);
  };

  document.querySelectorAll("[data-card-link]").forEach(card => {
    if (!(card instanceof HTMLElement) || card.dataset.cardBound === "true") return;
    card.dataset.cardBound = "true";

    card.addEventListener("click", event => {
      if (hasSelectedText()) return;
      if (isInteractiveElement(event.target, card)) return;
      activateCard(card);
    });

    card.addEventListener("keydown", event => {
      if (event.key !== "Enter" && event.key !== " ") return;
      if (isInteractiveElement(event.target, card)) return;
      event.preventDefault();
      activateCard(card);
    });
  });
</script>
